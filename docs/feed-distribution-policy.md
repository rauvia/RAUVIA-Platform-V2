# Política de Feeds

El blog provisto por WordPress generará su propio feed RSS. 
Regla: El feed RSS solo debe exponer posts con `status=publish` y debe ser enlazado desde la etiqueta `<link rel="alternate" type="application/rss+xml">` en el frontend de React.
